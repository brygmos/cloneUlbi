import React, {useContext} from 'react';
import {useState} from "react";

const Profile = () => {
    return (
        <h1>
            Личный кабинет
        </h1>
    );
};

export default Profile;
